define(['jquery', 'jquery-ui', 'selectable', 'popup'], function ($) {
  $(function() {
      $(document).on('focus', '#site_search', function () {
          // console.log($(this));
          // $(this).val('');
      });
  });
});
